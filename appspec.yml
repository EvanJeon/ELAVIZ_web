#602
version: 0.0
os: linux
files:
    - source: /
      destination: /home/ubuntu/build/
    - source: /index.html
      destination: /var/www/html/

permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu